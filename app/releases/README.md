# App Releases

This folder is reserved for local build artifacts.

Do not commit large binaries directly to git.

Preferred approach:
1. Build APK locally/CI.
2. Attach APK to GitHub Releases (for example, `v1.0.0`) as a release asset.
